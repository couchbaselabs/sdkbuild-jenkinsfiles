{
  "name": "@couchbase/sdk-ci-nodejs",
  "version": "0.1.0",
  "private": true,
  "description": "CI-core for the Couchbase Node.js SDK (couchnode) — neutral engine + Jenkins adapter + task executors. Shares no code with couchbase-sdk-ci/python/, only the pattern (see ../CONVENTIONS.md).",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test tests/*.mjs"
  },
  "dependencies": {
    "yaml": "2.9.0"
  }
}
